tip_input = raw_input('How much did you tip?')

tip = float(tip_input)

bill_input = raw_input("How much was the bill?")

bill = float(bill_input)

tip_ratio = tip / bill

tip_ratio less than 0.10:
    say "stingy"
tip_ratio greater than 0.20:    
    say "generous!"
otherwise:
    say "average"
    3 < 5 
